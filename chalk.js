/**
 * Make this scrip work!
 * Start with npm init
 * Then npm install –save <name of the module you need>
 * Take a look at the package.json created
 */
const chalk = require('chalk');
console.log(chalk.blue('Hello world!'));